name: Lessons

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: actions/setup-go@v5
        with:
          go-version-file: advanced/services/go.mod
          cache-dependency-path: advanced/services/go.sum
      - uses: gradle/actions/setup-gradle@v4

      - name: Go tests
        working-directory: advanced/services
        run: go vet ./... && go test ./...

      - name: Java tests
        working-directory: advanced
        run: ./gradlew test

      - name: Build all lessons
        run: advanced/scripts/build.sh

      - name: Smoke test lessons
        run: |
          advanced/scripts/smoke.sh 000-baseline
          advanced/scripts/smoke.sh 001-auto
